{
  "name": "litbible",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build:topics": "node scripts/build-topics-index.mjs",
    "build": "npm run build:topics && astro build && pagefind --site dist",
    "preview": "npm run build && astro preview"
  },
  "dependencies": {
    "astro": "^5.17.1",
    "simple-icons": "^16.7.0"
  },
  "devDependencies": {
    "pagefind": "^1.0.0"
  }
}
