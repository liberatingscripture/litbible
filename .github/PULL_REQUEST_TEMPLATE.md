## What changed

<!-- Brief description of the change and why. -->

## Checklist

- [ ] `npm run validate:chapters` passes (if any file under `src/data/chapters/` was touched)
- [ ] `npm run build` passes
- [ ] Scope stays inside the agreed area — no unrelated changes bundled in
