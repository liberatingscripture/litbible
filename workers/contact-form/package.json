{
  "name": "litbible-contact-form",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker backend for the litbible.net contact form (Turnstile verify + Email Routing send).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "check": "wrangler deploy --dry-run --outdir=.wrangler-check"
  },
  "dependencies": {
    "mimetext": "^3.0.27"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "vitest": "^5.0.0",
    "wrangler": "^4.127.0"
  }
}
